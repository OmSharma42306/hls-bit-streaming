const Header = () => (
  <div className="text-center mb-8">
    <h1 className="text-3xl font-bold text-gray-800">HLS Bit Streaming</h1>
    <p className="text-lg text-gray-600">Upload your video file to convert into HLS streams.</p>
  </div>
);

export default Header;
